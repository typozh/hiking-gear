<% if object.errors.any? %>
  <div class="error-messages">
    <h4><%= pluralize(object.errors.count, "error") %> prevented this from being saved:</h4>
    <ul>
      <% object.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
